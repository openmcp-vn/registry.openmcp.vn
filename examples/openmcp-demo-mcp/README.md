# OpenMCP-VN Demo MCP

Minimal stdio MCP server used to verify the OpenMCP-VN publish flow.

## Install

```bash
npm install -g @openmcp-vn/openmcp-demo-mcp
```

## Run

```bash
openmcp-demo-mcp
```

This package contains no credentials and exposes one tool: `hello`.
